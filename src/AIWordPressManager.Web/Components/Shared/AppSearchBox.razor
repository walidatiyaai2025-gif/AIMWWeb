<div class="app-search-box @Class" aria-busy="@Busy">
    <span class="app-search-box__icon" aria-hidden="true">⌕</span>
    <input type="search"
           value="@Value"
           placeholder="@Placeholder"
           aria-label="@AriaLabel"
           autocomplete="@AutoComplete"
           disabled="@Disabled"
           @oninput="HandleInput" />
    @if (Busy)
    {
        <span class="app-search-box__spinner" aria-hidden="true"></span>
        <span class="sr-only" role="status" aria-live="polite">@BusyText</span>
    }
    else if (!string.IsNullOrWhiteSpace(Value) && ShowClearButton)
    {
        <button type="button" class="app-search-box__clear" @onclick="ClearAsync" aria-label="@ClearAriaLabel">×</button>
    }
</div>

@code {
    [Parameter] public string Value { get; set; } = string.Empty;
    [Parameter] public EventCallback<string> ValueChanged { get; set; }
    [Parameter] public string? Placeholder { get; set; }
    [Parameter] public string? AriaLabel { get; set; }
    [Parameter] public string ClearAriaLabel { get; set; } = "Clear search";
    [Parameter] public string BusyText { get; set; } = "Searching";
    [Parameter] public string AutoComplete { get; set; } = "off";
    [Parameter] public string Class { get; set; } = string.Empty;
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public bool Busy { get; set; }
    [Parameter] public bool ShowClearButton { get; set; } = true;

    private async Task HandleInput(ChangeEventArgs args)
    {
        Value = args.Value?.ToString() ?? string.Empty;
        await ValueChanged.InvokeAsync(Value);
    }

    private async Task ClearAsync()
    {
        Value = string.Empty;
        await ValueChanged.InvokeAsync(Value);
    }
}
