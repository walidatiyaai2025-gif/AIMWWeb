# AI WordPress Manager v155.93.4

## Test database isolation

- Added configurable database paths to `ExecutionCenterService` and `ApprovalWorkflowService`.
- Added options to disable Execution Center seed data and the background timer.
- Updated workflow tests to use a unique temporary directory per test instance.
- Tests no longer delete or access the application's `%LocalAppData%` SQLite files.
- Added safe temporary-directory cleanup with short retries for SQLite sidecar files.
- Preserved the default application behavior when no custom options are provided.
- Restored the Execution Center enqueue overload used by Approval Workflow correlation and idempotency metadata.
