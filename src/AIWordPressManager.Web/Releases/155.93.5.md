# AI WordPress Manager v155.93.5

## Test corrections

- Updated `AuthUserTests` to reflect the approved registration model: new accounts default to `User`, while administrator accounts must request the `Administrator` role explicitly.
- Updated `WordPressContentStoreTests` to create a valid parent `Site` before persisting WordPress cache records.
- Prevents SQLite foreign-key failures from masking the snapshot behavior under test.
- Added an explicit administrator-role constructor test.

## Verification

Run a clean build and test. If workflow tests still fail, capture detailed output for `ApprovalWorkflowTests` and `ExecutionCenterTests`; their test names alone do not expose the remaining exception.