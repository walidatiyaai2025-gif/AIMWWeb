# AI WordPress Manager 155.89.1

## Taxonomy Explorer runtime fix

- Replaced the cross-table EF Core `Concat` query used by the global taxonomy explorer.
- Categories and tags are now queried independently and merged after materialization.
- Preserved owner isolation, site workspace filtering, search, minimum usage, sorting, summaries, and pagination.
- Prevents SQLite translation failures when opening `/module/taxonomy`.
