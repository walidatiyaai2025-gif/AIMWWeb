# AI WordPress Manager Web 155.86.0

## Workspace-aware content navigation

- Preserves the current workspace when switching between owned WordPress sites.
- Keeps site-specific routes on the equivalent page for the newly selected site.
- Adds or replaces the `siteId` query parameter on global module pages.
- Removes the workspace query cleanly when `All sites` is selected.
- Posts Explorer now reads the workspace `siteId` automatically.
- Pages Explorer now reads the workspace `siteId` automatically.
- Workspace selection remains isolated per signed-in user in browser storage.

Branch: `main`
