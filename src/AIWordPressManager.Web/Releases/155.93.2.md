# AI WordPress Manager Web 155.93.2

Branch: `main`

## Build fixes

- Fixed the comments grid sort selector to return the string type expected by `AppDataGrid`.
- Qualified `WordPressUserView` in the users workspace code-behind so the result constructor receives `IReadOnlyList<AIWordPressManager.Web.Services.WordPressUserView>`.
- This is a compile-fix patch; runtime verification still requires a clean local build.
