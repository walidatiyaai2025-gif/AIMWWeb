# 155.71.0 — Global Content Management Hub

Branch: `agent/logs-error-center`

- Added a unified content hub at `/content` and `/module/content`.
- Added cross-site totals for posts, pages, media, categories, and tags.
- Added per-site cached record counts, published and draft post counts, and last synchronization time.
- Added cache freshness classification: Fresh, Review, Stale, and No data.
- Added warnings for sites with stale or missing local data.
- Added direct links to global explorers, synchronization, per-site content explorer, and offline snapshot.
- Kept the feature backed entirely by the existing SQLite cache.
- Kept live WordPress comments separate because comments do not currently have a local cache table.
- Build identity: `v155.71.0 • agent/logs-error-center`.
