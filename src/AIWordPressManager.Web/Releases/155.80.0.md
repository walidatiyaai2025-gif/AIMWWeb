# AI WordPress Manager 155.80.0

Branch: `main`

## First-run onboarding

- Accounts with no owned WordPress sites are redirected to `/sites/connect` immediately after successful login.
- Existing users with sites continue to their requested or last visited page.
- The redirect is enforced server-side after authentication and ownership lookup.
- The existing guided site onboarding workflow remains responsible for creating and connecting the user's first site.
