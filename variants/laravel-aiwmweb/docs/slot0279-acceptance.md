# Slot0279 acceptance — AIMW-AUTO-148B15F121

Canonical operation: `AIMW-AUTO-148B15F121`
Canonical source: `src/AIWordPressManager.Web/Components/Pages/ExecutionCenter.razor`
Laravel scope: Execution Center empty-site connect control.

Acceptance contract:

- The control is rendered only after the authoritative tenant sites endpoint proves that the current tenant owns zero sites.
- The destination is tenant-scoped `/tenants/{tenant}/sites/connect` and retains the canonical English `Connect site` / Arabic `إضافة موقع` copy.
- Execution-view and site-management permissions are required; missing permissions fail closed without a sites request.
- Missing site endpoint, malformed responses, and read failures fail closed and do not synthesize an empty state.
- Existing tenant sites suppress the connect control.
- The control is tagged with the canonical operation ID and mounted only on the Laravel Execution Center route.

Focused deterministic coverage lives in `backend/resources/js/__tests__/execution-connect-site-control.test.tsx`.

This file records the acceptance contract only. Canonical TERMINAL status/counts are generated by the repository parity tooling after exact-head validation; this document does not claim parity completion by itself.
