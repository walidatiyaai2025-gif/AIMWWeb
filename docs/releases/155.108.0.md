# 155.108.0 - SQLite Setup Migration Fix

- Fixed setup failure `SQLite Error 1: 'no such column: OwnerUserId'` during database migration.
- Removed the premature `OwnerUserId` dependency from `20260808022000_AllowDuplicateSiteProfiles`.
- Finalized the owner/profile indexes only after the SQLite compatibility layer ensures the column exists.
- Preserved existing site data and duplicate-profile behavior.
- Added regression coverage for full fresh SQLite initialization.
