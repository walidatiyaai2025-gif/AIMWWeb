# AI WordPress Manager 155.94.10

Maintenance patch only. No new features were added.

## Fixed

- Prevented stale automation schedule loads from replacing newer state.
- Prevented background refresh callbacks from updating a disposed Blazor component.
- Prevented language-change rendering after the schedules workspace is closed.
- Added safe busy-state handling for refresh, save, edit, queue, toggle, and delete actions.
- Prevented duplicate schedule save and refresh operations.
- Preserved ownership checks before every schedule mutation.

## Verification

Run a clean Release build and the full test suite, then smoke-test `/module/schedules` by refreshing, saving, toggling, deleting, and navigating away while a refresh is active.
