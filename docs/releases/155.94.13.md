# AI WordPress Manager 155.94.13

Maintenance patch only. No new product features were added.

## Fixes

- Configuration validation failures are now handled instead of escaping into the Blazor circuit.
- The validation page shows an explicit error state when a validation run fails instead of remaining on the preparation state.
- Copy-report clipboard failures are handled safely, including disconnected JavaScript interop during navigation.
- Copy report is disabled until a validation report exists.

## Validation focus

- Open `/module/configuration` and rerun validation.
- Verify a failed validation is surfaced as an in-page error without an unexpected circuit failure.
- Test copying the report and navigating away immediately afterward.
