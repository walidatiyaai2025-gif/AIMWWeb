# Release 155.136.0 — UX-006 Dense Data Workspaces

## Summary

Release `155.136.0` completes UX-006 with 100 tracked implementation tasks focused on operational data tables, filters, selection scope, bulk actions, responsive density, and scanability.

## Shared data workspace improvements

- Expanded `AppDataGrid` with compact/comfortable/spacious density, optional striping, sticky headers, focusable viewport/rows, explicit caption/result semantics, and row-state metadata.
- Added external filter predicates that combine with grid search while preserving a single source collection.
- Added active-filter summaries, clear-all recovery, and distinct empty-data versus filtered-no-results states.
- Added visible-page selection, select-all-filtered scope, hidden-selected reporting, stale selection reconciliation, and optional integrated bulk actions.
- Improved sort-direction naming, pagination reset behavior, page-size normalization, and filtered/sorted CSV export.

## Filters and bulk actions

- Added shared `AppFilterBar` and removable `AppFilterChip` components.
- Hardened `AppBulkActionBar` with accessible region/busy semantics, sticky safe-area placement, scope guidance, dangerous treatment, secondary actions, and clear-selection support.
- Expanded responsive, RTL/LTR, reduced-motion, forced-colors, keyboard-focus, and non-color state styling.

## AI Usage adoption

- Migrated site filtering to the shared filter bar/chip contract while preserving account-scoped service authorization.
- Migrated recent AI usage activity to `AppDataGrid` with search, CSV export, compact density, success/error row states, localized pagination, and mobile cards.
- No usage persistence, tenant, authentication, AI runtime, or service contract changed.

## Validation

Implementation head `fe507dff21b68b5f27f5e0a6ac7e27efe672958d`:

- Build #1435 — SUCCESS.
- .NET Build Verification #1043 — SUCCESS.
- Automated tests: 300 passed, 0 failed, 0 skipped.
- Test artifact #9071148244 — 73,890 bytes.
- SHA-256: `29bce85608cbf4d320621bc844357d2444475baf97e822c99766110f6c9e4204`.
- Build: 0 errors; one pre-existing CS8604 warning remains in `Services/PublicEntryRouting.cs`.

## Next

UX-007 — Loading, empty, success, warning, offline & error states.
