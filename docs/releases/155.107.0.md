# 155.107.0 - Site Profile Identity Fix

- Same normalized WordPress URL can now be registered multiple times as independent site profiles.
- Removed the application-level duplicate URL rejection.
- Removed the legacy database uniqueness constraint on `Sites.SiteUrl` through migration `20260808022000_AllowDuplicateSiteProfiles`.
- Preserved per-profile ownership and credentials isolation.
- Added QA acceptance criteria for duplicate URL profiles.
- Corrected the build identity to release version `155.107.0`.
