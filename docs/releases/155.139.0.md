# Release 155.139.0 — UX-009 page hierarchy & consistency

## Summary

UX-009 closes the page-by-page visual hierarchy and consistency audit with exactly 100 completed code tasks. The release standardizes how production workspaces compose page intros, sections, cards, statistics, states and responsive action clusters while preserving existing application/domain behavior.

## Delivered

- Added `AppPage` as the shared page composition wrapper with page identity, width/density modes and passthrough metadata.
- Standardized `AppToolbar`, `AppCard` and `AppSection` heading hierarchy, semantic tones, density and ARIA title/description relationships.
- Extended `AppStatCard` with normalized tone/density, accessible naming, bidi-safe values and optional metadata.
- Added `page-consistency.css` after feedback states and before the UX-008 RTL/LTR final layer.
- Migrated Dashboard, Build & Release Notes, Account Profile, Account Email Settings, System Health, AI Prompt Templates and AI Center to the shared hierarchy.
- Preserved Dashboard refresh/timer logic, account security/mail services, health diagnostics, prompt versioning, AI orchestration and approval submission contracts.
- Preserved the UX-008 literal `data-bidi-scope="build-info"` contract through `AppPage` unmatched-attribute passthrough.
- Added `PageConsistencyUxContractTests` and an exact 100-task manifest guard.

## Validation

Stable implementation head `9584447199fd80daf16e305dc961b01103ca01e4`:

- Build #1457 — SUCCESS.
- .NET Build Verification #1065 — SUCCESS.
- Automated tests — 334 passed, 0 failed, 0 skipped.
- Build — 0 errors.
- Test artifact #9091966297 — 83,088 bytes.
- SHA-256 — `0e1267e72c21dbe4f8bf873151e7d2f50311b08da3f07cb0800cdbb58df63fa6`.
- One pre-existing CS8604 warning remains in `Services/PublicEntryRouting.cs`; UX-009 does not modify that service.

## Compatibility boundary

No database schema, tenant ownership, authentication model, API contract, AI routing, approval semantics, persistence contract, WordPress execution contract or security boundary is intentionally changed.

Browser-driven screenshot/visual-diff and automated axe regression gates remain UX-010 scope.